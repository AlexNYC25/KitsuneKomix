# Kitsune-Komix Config

Internal Monorepo package to parse the config values from the enviornment + Docker configs and returning it back after properly parsing and validating the values for the other packages/service in the monorepo
