
/**
 * AppMetadataConfig defines the structure for application metadata configuration, including optional fields for name, version, and description of the application.
 */
export type AppMetadataConfig = {
	name?: string;
	version?: string;
	description?: string;
};
